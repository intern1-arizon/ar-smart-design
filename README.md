
  # AR Smart

  This is a code bundle for AR Smart. The original project is available at https://www.figma.com/design/ljSIEvZ4ynvWNlNNk6fC5S/AR-Smart.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  